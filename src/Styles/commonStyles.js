const styles = (theme) => ({
    divRow: {
        display: 'flex',
        flexDirection: 'row'
    },
    divColumn: {
        display: 'flex',
        flexDirection: 'column'
    },
    divCenter: {
        justifyContent: 'center',
        alignItems: 'center'
    }
})

export default styles